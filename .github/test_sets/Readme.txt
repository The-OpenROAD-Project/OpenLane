Each test set is split across N files, where N is the number of parallel runners in the CI config.

Each file aims to roughly have the same design total runtime.

You can get the full test set via calling get_test_set.py.